import haulage.project.Location

model {
  Iterable<Location> locationList
  Integer total
}

json g.render(locationList, [pagination: [total: total, resource: Location, max: 10]])

//json tmpl.location(locationList ?: [])

