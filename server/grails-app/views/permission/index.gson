import haulage.project.Permission

model {
	Iterable<Permission> permissionList
	Integer total
}

//json tmpl.permission(permissionList ?: [])

json jsonapi.render(permissionList, [pagination: [total: total, resource: Permission]])


