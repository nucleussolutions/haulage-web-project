import haulage.project.TransportRequest

model {
	Iterable<TransportRequest> transportRequestList
	Integer total
}

json g.render(transportRequestList, [pagination: [total: total, resource: TransportRequest, max: 10], deep: true])
//json tmpl.transportRequest(transportRequestList ?: [])

